# Используем официальный образ Node.js
FROM node:18-alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем package.json и устанавливаем зависимости для серверной части приложения
COPY package*.json ./
RUN npm install

# Копируем все файлы приложения в контейнер (включая src)
COPY ./src .

# Открываем порт сервера (например, 5000)
EXPOSE 3000

# Запускаем сервер Node.js, указывая путь к app.js в папке src.
CMD ["node", "app.js"]
